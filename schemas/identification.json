{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "const": "identify"
    },
    "identity": {
      "description": "string to identify",
      "type": "string",
      "pattern": "^[0-9a-z]+$",
      "minLength": 1,
      "maxLength": 32
    },
    "key": {
      "descrption": "public key to identify",
      "type": "string",
      "pattern": "^[0-9a-f]{32}$"
    },
    "uris": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "uri"
      }
    }
  },
  "required": [
    "type",
    "identity",
    "key",
    "uris"
  ],
  "additionalProperties": false
}
